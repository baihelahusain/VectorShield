# VectorShield
VectorShield is a backend middleware/proxy designed to prevent Personally Identifiable Information (PII/PHI) from being permanently stored inside vector databases used in Retrieval-Augmented Generation (RAG) systems.
